name: Test level warning and nofilter mode
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Create ansible playbook for linting
        run: |
          mkdir -p "$GITHUB_WORKSPACE/tests/fixtures/ansible-warning"
          cat > "$GITHUB_WORKSPACE/tests/fixtures/ansible-warning/playbook.yml" << 'EOF'
          ---
          - name: Example playbook
            hosts: localhost
            gather_facts: false
            tasks:
              - name: Print hello
                ansible.builtin.debug:
                  msg: "Hello, World!"
          EOF

      - id: action
        uses: .
        with:
          github_token: ${{ github.token }}
          level: warning
          reporter: local
          filter_mode: nofilter
          fail_level: none
        continue-on-error: true

      - name: Assert action ran with warning level
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" = "success" ] || [ "$OUTCOME" = "failure" ]; then
            echo "PASS: action ran with warning level (outcome=$OUTCOME)"
          else
            echo "FAIL: unexpected outcome '$OUTCOME'"
            exit 1
          fi
